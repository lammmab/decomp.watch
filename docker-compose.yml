services:
  decomp:
    image: decomp-bot:latest
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file: .env
    environment:
      SNOOPLOGG: "*"
    volumes:
      - ./data:/data
    restart: unless-stopped
